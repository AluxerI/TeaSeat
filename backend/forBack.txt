docker exec -it teaset-backend sh


docker run -d -p 3000:80 teaseat-frontend 
запустить image -d ~ фоново от shell;  -p ~ publiced; 3000:80 внешний : внутрненний; имя


docker ps -a               = контенейры
docker images              = image 


apt-get update -y
apt-get install -y iputils-ping    === две команды для установки команды ping на docker


docker [compose] build ./../. -t nameImage:4.1.3            = 4.1.3 - опциональный тэг 



psql -U postgres -d teaseat
CREATE USER my_user WITH PASSWORD '102101' SUPERUSER;




docker-compose up --force-recreate --build -d            === пересобрать image   

docker-compose build --no-cache                          === пересобрать без кеша А ещё надо подумать по поводу pull


Если бд делает мозги, удали папку data!!!
Если бд делает мозги, удали папку data!!!
Если бд делает мозги, удали папку data!!!
Если бд делает мозги, удали папку data!!!

для развёртки
----
ssh -p 443 -o ServerAliveInterval=30 -o ServerAliveCountMax=3 -R0:127.0.0.1:8080 -t free.pinggy.io x:https
---
 

docker compose --profile test up -d pgsql_test
Для поднятия тестов


Скрипт:
-------------------------
# Зайди в контейнер
docker exec -it backend bash

# 1. ОЧИЩАЕМ ВСЁ
php artisan optimize:clear
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan filament:clear-cached-components

# 2. СОЗДАЕМ СИМЛИНК (если не создан)
php artisan storage:link

# 3. КЕШИРУЕМ ЗАНОВО
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan filament:cache-components


# 4. ОПТИМИЗИРУЕМ АВТОЗАГРУЗКУ
composer dump-autoload -o

# Выйди
exit
------------------------------------

php artisan octane:reload    - обновить код на сервере

